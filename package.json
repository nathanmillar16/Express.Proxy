{
  "name": "nathan.express.proxy",
  "version": "1.0.0",
  "description": "Package to proxy express requests, intercepting them and applying request headers",
  "main": "index.js",
  "scripts": {
    "start:header": "cd ../Next.Ecommerce.UI/apps/MFE/header && npm run start:headless:container",
    "start:proxy": "node index.js",
    "start": "npm-run-all --parallel start:proxy start:header",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathan J Millar",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "http-proxy": "^1.18.1",
    "npm-run-all": "^4.1.5"
  }
}
